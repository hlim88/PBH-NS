
enum primitiveVar {
	REST_DENSITY, //rest mass density
	SPECIFIC_ENERGY, //specific internal energy
	VELOCITY  //Radial component of Eulerian velocity 
};

enum conservativeVar {
	CONS_DENSITY,
	PHI,
	PI
};

static int numVariables = 3;

